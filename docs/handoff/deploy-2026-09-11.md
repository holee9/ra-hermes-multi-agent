# 배포 절차 — 2026-09-11 (사용자 실터미널 실행)

> ## ⛔ 보류 중 — 실행하지 말 것 (2026-09-11)
>
> 사용자 결정: **배포 중단, 먼저 정리.** 배포 매니페스트에 포함된 `knowledge_fetch.py` 가
> `docs/specs/llm-wiki-operating-model.md` §역할 경계 [HARD] 의 *"tree 순회·전체 임베딩 우회 ✕"* 와
> 충돌한다(#108). `b6d47bb`·`e519986` 이 truncation 을 뚫으려고 tree API 를 페이지당 순회한다.
> 이대로 배포하면 규칙 위반이 운영에 반영된다.
>
> **해소 후 이 배너를 지우고 진행한다.**

> 사용자 결정(2026-09-11): **지금 진행**. Claude 세션은 `/opt/hermes-ra/scripts` 쓰기와 `systemctl` 시스템 버스 접근이 막혀 있어 실행할 수 없다. 이 문서는 그대로 따라 실행하도록 정리한 것이다.
> 대상 호스트: **T3610** (Honcho·API 서버 호스트)

## 0. 무엇이 나가는가

`scripts/hermes-api-server.py` 를 건드린 커밋 9건이 배포 대상이다. 아래는 배포본에 반영되지 않은 것들이다.

| 커밋 | 내용 | 성격 |
|---|---|---|
| `27aae3d` | 완결성 판정을 종료코드 → Contract A 파서로 | **안전 필수** |
| `7912636` | (종료코드 계약 관련) | **단독 배포 금지** |
| `732e012` | #103 성장 리포트 읽기 전용 엔드포인트 | 신규 기능 |
| `60c2853`·`e3d336a` | 빈 `wp_comment` 살리기 차단 + Contract A 검증기 | 결함 수정 |
| `06ebc4a` | #103 경로 진단 + confidence 오버플로 | 결함 수정 |
| `f508699`·`a515ca9` | 출처 없는 고신뢰 자문 차단 + 비순회 evidence 500 | **결함 수정(중요)** |
| `2ce314c`·`876f602` | 요청 본문 입력 검증 (500 → 400) | 결함 수정 |

### [HARD] 배포 순서 제약

**`7912636` 을 단독으로 배포하지 않는다.** 현재 배포본은 종료코드를 보지 않고 stdout 을 채택한다. `7912636` 만 올리면 편재하는 SIGABRT(134) 때문에 **mail-triage 전건이 실패로 전환**된다. 반드시 `27aae3d` 와 함께 나가야 한다.

지금은 둘 다 `main` 에 있으므로 **`main` 을 통째로 배포하면 이 제약은 자동으로 충족**된다. 부분 배포·체리픽만 하지 않으면 된다.

## 1. 사전 확인

`git fetch` 는 원격 추적 ref 만 갱신하고 **작업 트리의 HEAD 를 옮기지 않는다.** 배포는 작업 트리 파일을 복사하므로, 확인해야 할 것은 원격이 아니라 **지금 체크아웃된 HEAD** 다.

```bash
cd ~/work/workspace-github/holee9/ra-hermes-multi-agent
git fetch origin main
git log --oneline -1                       # 지금 배포될 커밋 — 3906368 이상이어야 한다
git rev-list --count --left-right origin/main...HEAD
#   0 0  = 원격과 동일 (정상)
#   N 0  = 원격이 앞섬 -> `git pull --ff-only` 로 맞춘 뒤 다시 확인
git status --short                         # 배포 대상 파일에 미커밋 변경이 없어야 한다
```

## 2. 백업 (12파일 매니페스트)

**실패하면 실제로 멈춰야 한다.** `echo` 만 하는 경고는 다음 줄을 막지 못하고, `set -u` 는 미정의 변수만 잡지 명령 실패를 잡지 않는다. 그래서 백업은 **스크립트 파일로 실행**한다 — `exit` 가 동작해야 "백업 실패 시 배포 불가" 가 성립한다.

아래를 그대로 붙여 넣어 스크립트를 만든다.

```bash
cat > /tmp/ra-backup.sh <<'SCRIPT'
#!/usr/bin/env bash
set -euo pipefail

SRC=/opt/hermes-ra
STAMP=$(date +%Y%m%d-%H%M%S)
BACKUP="${SRC}/.backup-${STAMP}"

sudo mkdir -p "${BACKUP}/scripts"
sudo cp -a "${SRC}/hermes-api-server.py" "${SRC}/ra_citation_lint.py" "${BACKUP}/"
sudo cp -a "${SRC}/scripts/." "${BACKUP}/scripts/"

# 내용 검증 — 개수가 아니라 **바이트 단위 동일성**을 본다.
# diff 는 다르면 0 이 아닌 코드로 끝나고, set -e 가 여기서 중단시킨다.
sudo diff -r "${SRC}/scripts" "${BACKUP}/scripts" >/dev/null
sudo diff "${SRC}/hermes-api-server.py" "${BACKUP}/hermes-api-server.py" >/dev/null
sudo diff "${SRC}/ra_citation_lint.py"  "${BACKUP}/ra_citation_lint.py"  >/dev/null

# 드롭인(환경 설정)도 함께 보존한다 — 4번에서 건드리므로 롤백 범위에 포함되어야 한다.
if [ -d /etc/systemd/system/hermes-api-server.service.d ]; then
  sudo cp -a /etc/systemd/system/hermes-api-server.service.d "${BACKUP}/dropin.d"
fi

echo "BACKUP=${BACKUP}"
SCRIPT
chmod +x /tmp/ra-backup.sh
```

실행하고, **종료코드가 0 일 때만 다음 단계로 간다.**

```bash
bash /tmp/ra-backup.sh && echo "백업 검증 통과 — 배포 진행 가능" \
                       || echo "*** 백업 실패 — 배포하지 말 것 ***"
```

출력된 `BACKUP=...` 경로를 **적어 두고**, 같은 셸에서 이어서 쓸 것이면 변수로 넣는다.

```bash
export BACKUP="/opt/hermes-ra/.backup-20260911-000000"   # 위에서 출력된 실제 STAMP 로 바꿀 것
ls -la "$BACKUP"
```

> 새로 추가되는 파일(예: 배포본에 없던 스크립트)은 백업 대상이 아니다 — 원본이 없기 때문이다. 롤백은 **기존 파일의 이전 내용 복원**까지만 보장하며, 신규 파일은 롤백 후에도 남는다. 문제가 되면 7번에서 수동으로 지운다.

## 3. 드라이런 후 배포

```bash
bash scripts/deploy-local.sh --dry-run     # 바뀌는 파일 목록 확인
bash scripts/deploy-local.sh               # 실제 동기화 (import smoke check 포함)
```

`deploy-local.sh` 는 설치 전에 임시 디렉터리에서 진입점 import 를 먼저 검증한다(#139). 여기서 실패하면 설치하지 않고 멈추므로, 실패 메시지를 그대로 보고 중단하면 된다.

**다만 일부 파일을 SKIP 하고도 종료코드 0 으로 끝날 수 있다.** 종료코드만 보지 말고 출력의 `SKIPPED` 목록을 읽고, 아래로 **목적지 파일이 실제로 갱신됐는지** 대조한다.

```bash
# 저장소 원본과 배포된 파일의 해시가 같은지 12개 전부 대조
for f in hermes-api-server.py ra_citation_lint.py peer-comment-poller.py knowledge_fetch.py \
         index_github_repos.py index_ra_knowledge.py nas_indexer_v2.py growth-metrics.py \
         meta_extractor.py extract_mail_qa.py; do
  a=$(sha256sum "scripts/$f" 2>/dev/null | cut -d' ' -f1)
  b=$(sudo sha256sum "/opt/hermes-ra/scripts/$f" 2>/dev/null | cut -d' ' -f1)
  [ -n "$a" ] && [ "$a" = "$b" ] && echo "OK   $f" || echo "DIFF $f  (repo=${a:0:8} target=${b:0:8})"
done
for f in hermes-api-server.py ra_citation_lint.py; do
  a=$(sha256sum "scripts/$f" | cut -d' ' -f1)
  b=$(sudo sha256sum "/opt/hermes-ra/$f" 2>/dev/null | cut -d' ' -f1)
  [ "$a" = "$b" ] && echo "OK   root/$f" || echo "DIFF root/$f"
done
```

`DIFF` 가 하나라도 나오면 재기동하지 말고 원인을 먼저 확인한다.

## 4. 환경 변수 — `GROWTH_REPORTS_DIR` [필수]

#103 동기화 엔드포인트가 **생산자와 같은 디렉터리**를 보게 해야 한다. 설정하지 않으면 배포본 기본값이 `/opt/reports` 를 가리켜, 고치려던 "조용한 0건" 이 API 에서 그대로 재현된다.

```bash
sudo systemctl edit hermes-api-server      # 드롭인 편집기
```

편집기에 아래를 넣는다. 경로는 **리포트를 실제로 생성하는 체크아웃**이다.

```ini
[Service]
Environment=GROWTH_REPORTS_DIR=/home/abyz-lab/work/workspace-github/holee9/ra-hermes-multi-agent/reports
```

확인:

```bash
ls -d /home/abyz-lab/work/workspace-github/holee9/ra-hermes-multi-agent/reports
ls /home/abyz-lab/work/workspace-github/holee9/ra-hermes-multi-agent/reports/growth-*.json | tail -3
```

## 5. 재기동

```bash
sudo systemctl daemon-reload
sudo systemctl restart hermes-api-server
sudo systemctl status hermes-api-server --no-pager | head -15
```

## 6. 검증 (순서대로)

```bash
# 키는 셸 히스토리·화면에 남기지 않는다. 아래 중 하나를 쓴다.
read -rs KEY && echo                     # 붙여넣기 후 Enter (화면에 안 보임)
# 또는 이미 .env 에 있으면:  KEY=$(sudo grep -oP '^API_SERVER_KEY=\K.*' /opt/hermes-ra/.env)

# 6-1 health
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8643/health        # 기대 200

# 6-2 입력 검증 — 500 이 아니라 400 이어야 한다 (2ce314c·876f602)
curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:8643/v1/chat/completions \
  -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' \
  -d '{"messages":[{"role":"user","content":123}]}'                          # 기대 400
curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:8643/v1/chat/completions \
  -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' -d '[1]'   # 기대 400

# 6-3 #103 엔드포인트 — dir_exists 가 true 여야 한다 (4번 설정이 먹었는지)
curl -s http://localhost:8643/v1/growth/reports -H "Authorization: Bearer $KEY" \
  | python3 -c 'import sys,json; d=json.load(sys.stdin); print("dir_exists=",d.get("dir_exists"),"count=",d.get("count"),"dir=",d.get("reports_dir"))'
# 기대: dir_exists= True, count>0
```

`dir_exists=False` 가 나오면 4번 경로가 틀린 것이다. 재기동 전에 경로를 고친다.

### 6-4 최종 증거 — n8n 실제 메일 1건

위 6-1~6-3 은 배포가 붙었다는 증거이고, **mail-triage 가 실제로 동작한다는 증거는 n8n 실제 메일 1건이 통과하는 것**이다. 그 전까지는 "배포됨" 이지 "동작 확인됨" 이 아니다.

**6-2 의 curl 400 은 n8n 의 오류 흐름 증거가 아니다.** 서버가 400 을 돌려준다는 사실만 보여 준다. n8n 이 400 을 만났을 때 재시도하는지·중단하는지·Yellow 로 보내는지는 **미검증**이며, 그 확인은 실제 워크플로 실행에서만 가능하다. 정상 메일은 `content` 가 문자열이라 이 경로에 걸리지 않는다.

## 7. 롤백

롤백도 **스크립트 파일로 실행**한다. `{ test && cp || true; }` 같은 형태는 `cp` 실패를 삼켜 그대로 재기동으로 넘어간다.

드롭인은 **원래 있었는지**에 따라 복원 방식이 다르다. 없던 곳에 머지 복사를 하면 원상복구가 아니라 새 파일이 남는다. 그래서 2번 백업이 남긴 `dropin.d` 유무를 기준으로 분기하고, **이번에 건드린 파일만** 되돌린다(무관한 설정은 지우지 않는다).

```bash
cat > /tmp/ra-rollback.sh <<'SCRIPT'
#!/usr/bin/env bash
set -euo pipefail

BACKUP="${BACKUP:-}"
DROPIN_DIR=/etc/systemd/system/hermes-api-server.service.d
DROPIN_FILE="${DROPIN_DIR}/override.conf"

[ -n "$BACKUP" ] || { echo "BACKUP 미설정 — 중단"; exit 1; }
[ -f "$BACKUP/hermes-api-server.py" ] || { echo "백업 파일 없음: $BACKUP — 중단"; exit 1; }

sudo cp -a "$BACKUP/hermes-api-server.py" "$BACKUP/ra_citation_lint.py" /opt/hermes-ra/
sudo cp -a "$BACKUP/scripts/." /opt/hermes-ra/scripts/

# 드롭인 복원은 **디렉터리 유무가 아니라 override.conf 파일의 사전 존재**로 분기한다.
# 반례(Codex): 배포 전에 10-existing.conf 만 있고 override.conf 는 없었던 경우,
# 디렉터리 기준으로 머지 복사하면 이번에 만든 override.conf 가 그대로 남는다.
if [ -f "$BACKUP/dropin.d/override.conf" ]; then
  sudo cp -a "$BACKUP/dropin.d/override.conf" "$DROPIN_FILE"      # 이전 내용으로 되돌림
elif [ -f "$DROPIN_FILE" ]; then
  sudo rm -f "$DROPIN_FILE"                                       # 원래 없던 파일 — 제거
fi
# 백업에 있던 다른 드롭인(10-existing.conf 등)은 건드리지 않는다 — 이번 배포가 만들지 않았다.
sudo rmdir "$DROPIN_DIR" 2>/dev/null || true                      # 비었을 때만 정리

# 복원 검증 — 다르면 0 이 아닌 코드로 끝나고 set -e 가 중단시킨다
sudo diff -r "$BACKUP/scripts" /opt/hermes-ra/scripts >/dev/null
sudo diff "$BACKUP/hermes-api-server.py" /opt/hermes-ra/hermes-api-server.py >/dev/null

# 드롭인 복원 검증 — 재기동 전에 확인한다(재기동 후 health 200 만 보고 완료라 하면 안 된다)
if [ -f "$BACKUP/dropin.d/override.conf" ]; then
  sudo diff "$BACKUP/dropin.d/override.conf" "$DROPIN_FILE" >/dev/null
else
  [ ! -f "$DROPIN_FILE" ] || { echo "override.conf 가 남아 있다 — 복원 실패"; exit 1; }
fi

sudo systemctl daemon-reload
sudo systemctl restart hermes-api-server

code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8643/health)
[ "$code" = "200" ] || { echo "health=$code — 수동 확인 필요"; exit 1; }
echo "롤백 완료 · 복원 검증 통과 · health=200"
SCRIPT
chmod +x /tmp/ra-rollback.sh
```

```bash
export BACKUP="/opt/hermes-ra/.backup-20260911-000000"   # 2번에서 출력된 실제 경로
bash /tmp/ra-rollback.sh
echo "rollback_exit=$?"      # 0 이 아니면 롤백이 끝나지 않은 것 — 수동 확인
```

> 2번에서 말한 대로, **신규 추가 파일은 롤백 대상이 아니다.** 원본이 없어 되돌릴 내용이 없다. 남아도 문제가 되는 파일이 있으면 이름을 확인해 수동으로 지운다.

## 8. 이번 배포에서 하지 않는 것

- `HIVE_SUBMIT_ENABLED` 를 설정하지 않는다 (기본 비활성 유지).
- `HIVE_LEDGER_PATH` 도 P3 파일럿 승인 전에는 설정하지 않는다.
- n8n 워크플로 JSON 은 건드리지 않는다 (G5 — 사전 보고 대상).
- `scripts/fetch-growth-reports.py` 는 **소비 호스트(raspi5p) 용**이라 T3610 매니페스트에 없다. raspi5p 배치는 별건이다.

관련: #103, #150, `docs/hive/p3-0-measurement.md` §4
